# פתרון בעיות Google Sheets API

## הבעיה: הקטגוריות לא נטענות מ-Google Sheets

### סיבות אפשריות:

1. **אין מפתח API** - הקוד יעבוד רק אם הגיליון ציבורי
2. **הגיליון לא משותף** - צריך לשתף עם "כל מי שיש לו קישור"
3. **מפתח API לא תקין** - צריך ליצור מפתח API ב-Google Cloud Console

---

## פתרון 1: שיתוף הגיליון (הכי פשוט)

1. פתח את Google Sheets שלך
2. לחץ על כפתור "שיתוף" (Share)
3. בחר "כל מי שיש לו קישור"
4. ודא שהרשאה היא "Viewer" (צופה)
5. לחץ "העתק קישור" ושמור את הקישור

**זה אמור לעבוד מיד ללא מפתח API!**

---

## פתרון 2: יצירת מפתח API (מומלץ לפרודקשן)

### שלב 1: יצירת פרויקט ב-Google Cloud

1. לך ל-[Google Cloud Console](https://console.cloud.google.com/)
2. לחץ על "Select a project" למעלה
3. לחץ על "New Project"
4. הזן שם לפרויקט (למשל: "Excel Analyzer")
5. לחץ "Create"

### שלב 2: הפעלת Google Sheets API

1. לחץ על "APIs & Services" בתפריט
2. לחץ על "Library"
3. חפש "Google Sheets API"
4. לחץ על "Google Sheets API"
5. לחץ על כפתור "Enable"

### שלב 3: יצירת מפתח API

1. לחץ על "APIs & Services" > "Credentials"
2. לחץ על "Create Credentials" > "API Key"
3. העתק את המפתח שנוצר
4. (אופציונלי) לחץ על "Restrict Key" כדי להגביל את המפתח רק ל-Google Sheets API

### שלב 4: הוספת המפתח לקוד

1. צור קובץ `.env` בתיקיית הפרויקט (אם עדיין לא קיים)
2. הוסף את השורה הבאה:
   ```
   VITE_GOOGLE_SHEETS_API_KEY=המפתח_שלך_כאן
   ```
3. הפעל מחדש את השרת (`npm run dev`)

---

## מבנה הגיליון

הקוד מצפה למבנה הבא ב-Google Sheets:

- **עמודה E (index 4)**: מספר זהות (מ.ז)
- **עמודה B (index 1)**: תאריך התנועה (חודש)
- **עמודה K (index 10)**: סכום התנועה
- **עמודה M (index 12)**: קטגוריה

**שורה ראשונה יכולה להיות כותרות, אבל הקוד משתמש באינדקסים קבועים!**

---

## בדיקת שגיאות

אם הקטגוריות עדיין לא נטענות:

1. פתח את הקונסול בדפדפן (F12)
2. חפש הודעות שגיאה
3. בדוק שהגיליון נגיש (נסה לפתוח אותו בדפדפן אחר/מודע)
4. ודא שה-ID של הגיליון נכון: `1ObJQ9qkwggN8xE7WmFOWZWQOCTSMTwaP2l5QdNl51sY`

---

## הודעות שגיאה נפוצות

### "שגיאת הרשאות: 403"
→ הגיליון לא משותף. פתח אותו ושתף עם "כל מי שיש לו קישור"

### "שגיאה בפרמטרים: 400"
→ ה-ID של הגיליון לא נכון. בדוק את הקישור

### "לא נמצאו נתונים"
→ הגיליון ריק או שהשורות לא מכילות את הנתונים הנדרשים

---

## סיוע נוסף

אם עדיין יש בעיות:
1. בדוק את הקונסול (F12) והודעות השגיאה
2. ודא שהגיליון נגיש
3. נסה לפתוח את הגיליון בדפדפן אחר כדי לוודא שהוא נגיש
