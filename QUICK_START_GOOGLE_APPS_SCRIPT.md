# 🚀 מדריך מהיר: הגדרת Google Apps Script לכתיבה

## ⚠️ בעיה: שגיאת 401 Unauthorized

כתיבה לגוגל שיטס דורשת אימות מתקדם. הפתרון הכי פשוט הוא **Google Apps Script**.

---

## 📝 שלבים (5 דקות)

### שלב 1: פתח את Google Apps Script

1. פתח את **הגיליון שלך** ב-Google Sheets
2. לחץ על **"Extensions"** (תוספים) בתפריט העליון
3. בחר **"Apps Script"**

### שלב 2: הדבק את הקוד

1. **מחוק** את כל הקוד הקיים (אם יש)
2. פתח את הקובץ **`google-apps-script-code.js`** בתיקיית הפרויקט
3. **העתק** את כל התוכן
4. **הדבק** ב-Apps Script editor
5. לחץ על **"Save"** (שמור) - תן שם לפרויקט (למשל "Excel Analyzer Writer")

### שלב 3: Deploy כח Web App

1. לחץ על כפתור **"Deploy"** (פרסום) למעלה
2. בחר **"New deployment"** (פריסה חדשה)
3. לחץ על האייקון ⚙️ ליד **"Select type"**
4. בחר **"Web app"** מהרשימה
5. הגדר את הפרטים הבאים:
   - **Description:** Excel Analyzer Write API (אופציונלי)
   - **Execute as:** **Me** (מבצע כמוני) ⚠️ חשוב!
   - **Who has access:** **Anyone** (כל מי שיש לו את הקישור) ⚠️ חשוב!
6. לחץ **"Deploy"**
7. **העתק את ה-Web App URL** שנוצר (נראה כך: `https://script.google.com/macros/s/ABC123.../exec`)

### שלב 4: הוסף את ה-URL לקוד

1. פתח את קובץ **`.env`** בתיקיית הפרויקט
   - אם הקובץ לא קיים, צור אותו בתיקייה הראשית של הפרויקט
2. הוסף את השורה הבאה (או עדכן אם כבר קיימת):
   ```
   VITE_GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
   ```
   **החלף `YOUR_SCRIPT_ID`** ב-URL שהעתקת בשלב הקודם
3. **שמור** את הקובץ

### שלב 5: הפעל מחדש את השרת

1. עצור את השרת (לחץ Ctrl+C בטרמינל)
2. הפעל מחדש: `npm run dev`
3. נסה לשלוח שורות לגוגל שיטס שוב

---

## ✅ בדיקה

אחרי ההגדרה, נסה לשלוח שורות חסרות לגוגל שיטס. אם הכל עובד:
- השורות יתווספו לגיליון
- תראה הודעת הצלחה
- השגיאת 401 לא תופיע יותר

---

## 🐛 פתרון בעיות

### "Authorization required"
- ודא שהגדרת **"Who has access: Anyone"** ב-Deployment
- ודא שהגדרת **"Execute as: Me"**

### "Script function not found"
- ודא שהדבקת את כל הקוד מקובץ `google-apps-script-code.js`
- ודא שיש לך פונקציה בשם `doPost`

### השגיאה 401 עדיין מופיעה
- ודא שה-URL ב-`.env` נכון (מתחיל ב-`https://script.google.com/macros/s/`)
- ודא שהפעלת מחדש את השרת אחרי הוספת ה-URL
- בדוק שהקובץ `.env` נמצא בתיקייה הראשית של הפרויקט

---

## 📞 עזרה נוספת

אם עדיין יש בעיות:
1. פתח את הקונסול בדפדפן (F12) ובדוק מה השגיאה המדויקת
2. ודא שהקוד ב-Apps Script נשמר ונפרס בהצלחה
3. נסה לפתוח את ה-URL של ה-Web App בדפדפן - אמור להציג JSON עם הודעה

---

## 🔒 אבטחה

**הערה חשובה:** ה-Web App URL נותן גישה לכתיבה לגיליון שלך. שמור אותו סודי ולא תפרסם אותו בפומבי.

אם ה-URL נחשף, תוכל:
1. לפתוח את Apps Script
2. לחץ על "Deploy" > "Manage deployments"
3. לחץ על האייקון ✏️ ליד ה-deployment
4. לחץ על "New version" כדי ליצור גרסה חדשה עם URL חדש
